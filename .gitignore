node_modules
dist
dist-ssr
*.local
.DS_Store
.vite

# build-time raw dictionary cache (not shipped)
.cache

# Xcode/Capacitor ビルド生成物（ソースではないので追跡しない）
build/
DerivedData/

# 署名鍵・パスワード（絶対にコミットしない。紛失しないよう別途バックアップすること）
*.jks
*.keystore
android/keystore.properties

# App Store撮影の生成物(大きいバイナリ・スクリプトで再生成可)
appstore-screenshots/

# 語彙拡張の中間生成物(再生成可能・大容量)
tools/expand/chunks/
tools/expand/audit/
