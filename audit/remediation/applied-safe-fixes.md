# 適用した安全な修正（第5段階）

適用日: 2026-07-15 / 方式: カテゴリAのうち適用条件を全て満たすもののみ / 復元: `git` 差分で可能

## 適用条件（全て満たすもののみ適用）

- 意味・語義・例文の創作を伴わない（純粋な正書法）
- 修正後の値が一意
- confidence ≥ 0.99
- 元データと監査内容が一致（実データで現在値を確認）
- 変更対象が明確
- JSON構造を壊さない
- 検証可能

## 適用した修正：3件（ドイツ語 普通名詞の語頭大文字化）

| id | field | before | after | 根拠 | confidence |
|---|---|---|---|---|---|
| de-00539 | prompt | `「raum」の意味は？` | `「Raum」の意味は？` | 独語普通名詞は語頭大文字 | 0.99 |
| de-00648 | prompt | `「weihnachten」の意味は？` | `「Weihnachten」の意味は？` | 同上 | 0.99 |
| de-00743 | prompt | `「abendessen」の意味は？` | `「Abendessen」の意味は？` | 同上 | 0.99 |

**重要（実データ照合の成果）**: 監査記録では `field: "headword"` とされていたが、実データにheadwordフィールドは存在せず、ドイツ語の見出し語は `prompt`（"「raum」の意味は？"）内に格納されていた。報告を鵜呑みにせず実データを確認したため、正しい `prompt` フィールドへ適用できた。answer/glosses/choices は英語（"room, space"等）で当該語を含まず、影響なし。

## 検証結果（全パス）

- 全JSONパース成功
- 総件数 = 20,131（不変）
- manifest件数一致
- 変更エントリの choices = 4件、正解が choices 内に存在、answer 不変
- **差分はde-00539/00648/00743のpromptのみ・他フィールド完全不変**（エントリ単位でHEADと厳密照合済み）
- `npx tsc -b` exit 0（型・構造破壊なし）

備考: 本プロジェクトには単体テストランナー（vitest/jest）が未導入・テストファイル0のため、検証はデータ整合性チェックとTypeScriptビルドで実施した（存在しないテスト結果は記載しない）。

## 適用を見送った「カテゴリA型」候補（≥0.99ゲート未達・要確認）

正書法として妥当だが confidence < 0.99 のため自動適用しなかったもの（人手/辞書確認へ）:

| id | before → after | confidence | 種別 |
|---|---|---|---|
| de-00823 | feind → Feind | 0.90 | 独名詞大文字化 |
| de-01797 | dutzend → Dutzend | 0.97 | 独名詞大文字化 |
| de-01223 | english → English | 0.97 | 英語正書法(言語名) glosses.en |
| de-01233 | sunday → Sunday | 0.97 | 英語正書法(曜日) glosses.en |
| de-01296 | saturday → Saturday | 0.97 | 英語正書法(曜日) glosses.en |
| zh-04089 他 punctuation計7 | 半角読点 `,` → 全角 `，`（例文） | 0.97 | 学習者可視の例文本文のため保留 |
| zh-01421/01497 | 拼音の分かち書き統一 | 0.90 | 分節規約に幅があり保留 |

これらは `category-A-safe.json` に含まれるが、ゲート（≥0.99）未達のため未適用。人手または辞書確認の上で別途適用を推奨。
